{% extends "layout.html.twig" %}

{% set navtab = 'users' %}

{% block content %}
<div class="container">
    <div class="col-md-9">
        <section class="well">
            <h3>Utilisateurs</h3>

            {% if users %}
            <div class="table-responsive">
                <table class="table table-hover table-striped">
                    <tr>
                        <th width="20"><input type="checkbox"></th>
                        <th>#</th>
                        <th>Email</th>
                        <th>Nom</th>
                        <th>Société</th>
                        <th></th>
                    </tr>
                {% for user in users %}
                    <tr>
                        <td><input type="checkbox"></td>
                        <td>{{ user.idUser }}</td>
                        <td><a href="{{ domain() }}user/{{ user.idUser }}/show">{{ user.email }}</a></td>
                        <td><a href="{{ domain() }}user/{{ user.idUser }}/show">{{ user.firstname ~ ' ' ~ user.lastname }}</a></td>
                        <td><a href="{{ domain() }}company/{{ user.company }}/show">{{ user.company }}</a></td>
                        <td align="right">
                            <a href="{{ domain() }}user/{{ user.idUser }}/show"><i class="fa-eye"></i></a>
                            <a href="{{ domain() }}user/{{ user.idUser }}/edit"><i class="fa-edit"></i></a>
                            <a href="{{ domain() }}user/{{ user.idUser }}/delete"><i class="fa-times"></i></a>
                        </td>
                    </tr>
                {% endfor %}
                </table>
            </div>
            {% else %}
            <div class="alert alert-warning">Aucun utilisateur</div>
            {% endif %}
        </section>
    </div>
    <div class="col-md-3">
        <a class="btn btn-lg btn-block btn-primary" href="{{ domain() }}user/add"><i class="fa-plus"></i>&nbsp;Ajouter</a>
    </div>
</div>
{% endblock %}