{% extends "user/users.html.twig" %}

{% block content %}
<div class="container">
    <div class="row">
        <div class="col-md-9">
            <section class="well">
                <h3>Détail utilisateur</h3>
                
                <p>{{ user.email }}</p>
            </section>
        </div>
        <div class="col-md-3">
            <a class="btn btn-lg btn-block btn-warning" href="{{ base_url() }}user/{{ user.idUser }}/edit">Modifier</a>
            <a class="btn btn-lg btn-block btn-danger" href="{{ base_url() }}user/{{ user.idUser }}/delete">Supprimer</a>
        </div>
    </div>
</div>
{% endblock %}